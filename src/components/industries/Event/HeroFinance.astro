<!-- Finance Hero Start -->
<section class="finance-hero">
  <div class="container">
    <div class="row align-items-center">
      <!-- Left Content -->
      <div class="col-lg-6 text-white text-lg-start text-center">
        <h1 class="finance-hero__title poppins-bold">
          Elevate Every Event with Powerful Digital Presence
        </h1>
        <p class="finance-hero__subtitle poppins-regular">
          From immersive websites to strategic social campaigns, we help event
          planners, organizers, and venues attract more clients, sell more
          tickets, and build unforgettable digital experiences. Whether it's a
          concert, corporate event, or wedding expo — we make your event shine
          online before it even begins.
        </p>
        <div class="tz-buttons d-flex justify-content-start">
          <a
            href="#"
            class="tz-button-white text-uppercase fw-medium tz-text-m"
          >
            Book a free consultation</a
          >
          <a class="tz-button-white-circle" href="#"
            ><i class="ph ph-arrow-up-right"></i></a
          >
        </div>
      </div>

      <!-- Right Form -->
      <div class="col-lg-5 offset-lg-1">
        <form class="finance-hero__form">
          <h4 class="mb-3 poppins-bold">Get a Free Consultation</h4>
          <input
            type="text"
            class="form-control mb-3"
            placeholder="Your Name"
            required
          />
          <input
            type="email"
            class="form-control mb-3"
            placeholder="Email Address"
            required
          />
          <input
            type="tel"
            class="form-control mb-3"
            placeholder="Phone Number"
            required
          />
          <button type="submit" class="btn w-100 poppins-bold">Submit</button>
        </form>
      </div>
    </div>
  </div>
</section>
<!-- Finance Hero End -->

<style>
  .finance-hero {
    background-image: url("/assets/images/blog/blog1-img2.jpg"); /* Change path */
    background-size: cover;
    background-position: center;
    min-height: 70vh; /* Full height */
    display: flex;
    align-items: center;
    color: #fff;
    position: relative;
  }

  .finance-hero::before {
    content: "";
    position: absolute;
    inset: 0;
    background: rgba(0, 0, 0, 0.5); /* Overlay */
    z-index: 1;
  }

  .finance-hero .container {
    position: relative;
    z-index: 2;
  }

  .finance-hero__title {
    font-size: 42px;
    font-weight: 700;
    margin-bottom: 20px;
  }

  .finance-hero__subtitle {
    font-size: 18px;
    /* max-width: 500px; */
    margin-bottom: 20px;
  }

  .finance-hero__form {
    background: #fff;
    border-radius: 12px;
    padding: 30px;
    box-shadow: 0 0 40px rgba(0, 0, 0, 0.2);
    color: #000;
    width: 285px;
    margin-left: 215px;
  }

  .finance-hero__form input {
    height: 48px;
  }

  .finance-hero__form h4 {
    font-weight: 600;
    margin-bottom: 1rem;
  }

  .btn {
    background-color: #caf301;
  }

  /* Responsive Adjustments */
  @media (max-width: 767px) {
    .finance-hero {
      padding: 60px 0;
      min-height: auto;
    }
    .finance-hero__title {
      font-size: 28px;
    }
    .finance-hero__subtitle {
      font-size: 16px;
    }
    .finance-hero__form {
      margin: 40px auto 0 auto; /* center form on small screens */
      width: 100%;
      max-width: 320px;
    }
  }
</style>
